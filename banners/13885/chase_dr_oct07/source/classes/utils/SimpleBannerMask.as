/** *	SimpleBannerMask Class, Created 2007-10-01 *	Copyright (c) 2007 T3. All rights reserved. *   *  Use to mask off the Stage area of the banner. *   *	@author Timothy Harold *	@version 1.0 *	*/class classes.utils.SimpleBannerMask{	// static properties			// private properties	private var __targetMask:MovieClip;			// public properties				// Class constructor	/**	 * Invoke the makeMask() method to set the dimensions of the banner's mask.	 * Requires a MovieClip instance that contains a vector rectangle embedded.	 * The makeMask() method will set the dimensions based on the banner's	 * Stage.width and Stage.height.	 *	 * @params _targetMask A MovieClip reference to the mask MovieClip.	 */	function SimpleBannerMask(_targetMask:MovieClip)	{		// reference to the mask MovieClip		__targetMask = _targetMask;	}				// private methods  ------------------------------------------------------------------------------			// public methods  -------------------------------------------------------------------------------	/**	 * Sets the dimensions of the banner's mask.	 * Requires a MovieClip instance that contains a vector rectangle embedded.	 * The makeMask() method will set the dimensions based on the banner's	 * Stage.width and Stage.height.	 */	public function makeMask():Void	{				// grab the Stage width and height		var maskWidth:Number = Stage.width;		var maskHeight:Number = Stage.height;				// set dimensions		__targetMask._width = maskWidth;		__targetMask._height = maskHeight;				// set to 0, 0		__targetMask._x = __targetMask._y = 0;	}		// handlers/listeners  ---------------------------------------------------------------------------					// end}